/**
 * DISABLED — No auto-generated spell content allowed.
 * Only hand-written, verified spells with proper citations are permitted.
 * 
 * To re-enable: Remove this file and restore from git history.
 */
console.log("[nightly-sync] DISABLED — Auto-generation and sync of spells is prohibited.");
console.log("[nightly-sync] Only manually verified spells with academic citations are allowed.");
process.exit(0);